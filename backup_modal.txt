               </button>
            </div>
          </div>
        )}
      </div>

      {/* Simplified Modal Overlays */}
      {/* Checkout Payment Flow Modal */}
      {/* Checkout Modal â€” two-column layout matching reference */}
      {showPayment && (
        <div className="fixed inset-0 z-[2000] bg-black/40 backdrop-blur-sm flex items-center justify-center p-4">
          <div className="flex shadow-2xl font-sans text-gray-800 rounded-2xl overflow-hidden" style={{ width: 'min(96vw, 1020px)', maxHeight: '92vh', backgroundColor: '#f0f0f0' }}>

            {/* â”€â”€ LEFT PANEL: Checkout Form â”€â”€ */}
            <div className="flex flex-col" style={{ flex: '1 1 0%', minWidth: 0, backgroundColor: '#f0f0f0', overflow: 'hidden' }}>
              {/* Header */}
              <div className="flex items-center gap-3 px-5 pt-5 pb-4">
                <button onClick={() => setShowPayment(false)} style={{ background: 'none', border: 'none', cursor: 'pointer', color: '#555', padding: '4px' }}>
                  <ArrowLeft size={20} />
                </button>
                <h2 style={{ margin: 0, fontSize: '17px', fontWeight: 700, color: '#111' }}>Checkout</h2>
              </div>

              {/* Scrollable left content */}
              <div className="flex-1 overflow-y-auto" style={{ padding: '0 16px 16px' }}>

                {/* CARD 1: Delivered Details + Choose delivery options */}
                <div className="bg-white rounded-2xl" style={{ padding: '20px', marginBottom: '12px', border: '1px solid #e8e8e8' }}>
                  {/* Delivered Details header */}
                  <div className="flex justify-between items-center mb-4">
                    <h3 style={{ margin: 0, fontSize: '15px', fontWeight: 700, color: '#111' }}>Delivered Details</h3>
                    <div className="flex gap-2">
                      <button style={{ padding: '5px 14px', borderRadius: '20px', border: '1px solid #ddd', fontSize: '12px', fontWeight: 600, background: '#fff', color: '#111', cursor: 'pointer' }}>Delivery</button>
                      <button style={{ padding: '5px 14px', borderRadius: '20px', border: '1px solid #ddd', fontSize: '12px', fontWeight: 600, background: '#fff', color: '#555', cursor: 'pointer' }}>Pickup</button>
                    </div>
                  </div>

                  {/* Address row */}
                  <div className="flex items-start gap-3 mb-3">
                    <div style={{ marginTop: 2, color: '#555', flexShrink: 0 }}><MapPin size={15} /></div>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div className="flex justify-between items-center">
                        <span style={{ fontWeight: 700, fontSize: '13px', color: '#111' }}>Midtown South, Manhattan</span>
                        <button style={{ color: '#ff6036', fontSize: '12px', fontWeight: 700, background: 'none', border: 'none', cursor: 'pointer' }}>Edit</button>
                      </div>
                      <p style={{ margin: '3px 0 0', fontSize: '11px', color: '#888', lineHeight: 1.5 }}>123 Madison Avenue, Apartment 12B, Midtown South, Manhattan, New York City, NY 10016, United States</p>
                    </div>
                  </div>

                  {/* Divider */}
                  <div style={{ height: 1, backgroundColor: '#f0f0f0', margin: '12px 0' }}></div>

                  {/* Name row */}
                  <div className="flex items-start gap-3 mb-4">
                    <div style={{ marginTop: 2, color: '#555', flexShrink: 0 }}><User size={15} /></div>
                    <div style={{ flex: 1 }}>
                      <div className="flex justify-between items-center">
                        <span style={{ fontWeight: 700, fontSize: '13px', color: '#111' }}>Mr. Azzahri Alpiana</span>
                        <button style={{ color: '#ff6036', fontSize: '12px', fontWeight: 700, background: 'none', border: 'none', cursor: 'pointer' }}>Edit</button>
                      </div>
                      <button style={{ color: '#888', fontSize: '11px', marginTop: 4, background: 'none', border: 'none', cursor: 'pointer', padding: 0, textDecoration: 'underline' }}>Add instruction for courier</button>
                    </div>
                  </div>

                  {/* Divider */}
                  <div style={{ height: 1, backgroundColor: '#f0f0f0', margin: '4px 0 16px' }}></div>

                  {/* Choose delivery options */}
                  <h3 style={{ margin: '0 0 12px', fontSize: '15px', fontWeight: 700, color: '#111' }}>Choose delivery options</h3>
                  <div style={{ display: 'flex', flexDirection: 'column', gap: '8px' }}>

                    {/* Express */}
                    <div onClick={() => setDeliveryOption('Express')} className="flex items-center justify-between cursor-pointer rounded-xl" style={{ padding: '12px 14px', border: deliveryOption === 'Express' ? '1.5px solid #2d9585' : '1px solid #e5e7eb', background: '#fff' }}>
                      <div className="flex items-center gap-3">
                        <div className="w-8 h-8 rounded-lg flex items-center justify-center" style={{ background: deliveryOption === 'Express' ? '#eaf6f4' : '#f5f5f5', color: deliveryOption === 'Express' ? '#2d9585' : '#aaa' }}>
                          <Zap size={15} fill={deliveryOption === 'Express' ? 'currentColor' : 'none'} strokeWidth={deliveryOption === 'Express' ? 0 : 2} />
                        </div>
                        <div>
                          <div className="flex items-center gap-2">
                            <span style={{ fontWeight: 700, fontSize: '13px', color: '#111' }}>Express</span>
                            <span style={{ backgroundColor: '#2d9585', color: '#fff', fontSize: '9px', fontWeight: 700, padding: '2px 7px', borderRadius: '999px' }}>Faster</span>
                          </div>
                          <p style={{ margin: '2px 0 0', fontSize: '11px', color: '#888' }}>Estimation 15â€“20 min</p>
                        </div>
                      </div>
                      <span style={{ fontWeight: 700, fontSize: '13px', color: '#111' }}>$1.99</span>
                    </div>

                    {/* Standard */}
                    <div onClick={() => setDeliveryOption('Standard')} className="flex items-center gap-3 cursor-pointer rounded-xl" style={{ padding: '12px 14px', border: deliveryOption === 'Standard' ? '1.5px solid #2d9585' : '1px solid #e5e7eb', background: '#fff' }}>
                      <div className="w-8 h-8 rounded-lg flex items-center justify-center" style={{ background: deliveryOption === 'Standard' ? '#eaf6f4' : '#f5f5f5', color: deliveryOption === 'Standard' ? '#2d9585' : '#aaa' }}>
                        <Archive size={14} strokeWidth={2} />
                      </div>
                      <div>
                        <div style={{ fontWeight: 700, fontSize: '13px', color: '#111' }}>Standard</div>
                        <p style={{ margin: '2px 0 0', fontSize: '11px', color: '#888' }}>Estimation 30â€“40 min</p>
                      </div>
                    </div>

                    {/* Economical */}
                    <div onClick={() => setDeliveryOption('Economical')} className="flex items-center gap-3 cursor-pointer rounded-xl" style={{ padding: '12px 14px', border: deliveryOption === 'Economical' ? '1.5px solid #2d9585' : '1px solid #e5e7eb', background: '#fff' }}>
                      <div className="w-8 h-8 rounded-lg flex items-center justify-center" style={{ background: deliveryOption === 'Economical' ? '#eaf6f4' : '#f5f5f5', color: deliveryOption === 'Economical' ? '#2d9585' : '#aaa' }}>
                        <Archive size={14} strokeWidth={2} />
                      </div>
                      <div>
                        <div style={{ fontWeight: 700, fontSize: '13px', color: '#111' }}>Ecomical</div>
                        <p style={{ margin: '2px 0 0', fontSize: '11px', color: '#888' }}>Estimation 50â€“60 min</p>
                      </div>
                    </div>

                  </div>
                </div>

                {/* CARD 2: Payment */}
                <div className="bg-white rounded-2xl" style={{ padding: '20px', marginBottom: '16px', border: '1px solid #e8e8e8' }}>
                  <h3 style={{ margin: '0 0 14px', fontSize: '15px', fontWeight: 700, color: '#111' }}>Payment</h3>
                  {!showPaymentOptions ? (
                    <div className="flex items-center gap-3">
                      <div className="rounded-xl flex items-center justify-center" style={{ width: 42, height: 38, background: '#fff3ef', border: '1px solid #fde0d4', color: '#ff6036', flexShrink: 0 }}>
                        <CreditCard size={18} strokeWidth={2.5} />
                      </div>
                      <div style={{ flex: 1, minWidth: 0 }}>
                        <div style={{ fontWeight: 700, fontSize: '13px', color: '#111', marginBottom: 2 }}>
                          {paymentMethod === 'Card' ? 'Add your debit card' : paymentMethod === 'UPI' ? 'Pay with UPI' : paymentMethod === 'Net Banking' ? 'Net Banking' : 'Cash on Delivery'}
                        </div>
                        <div style={{ fontSize: '11px', color: '#888' }}>
                          {paymentMethod === 'Card' ? 'You can use debit card for continue your payment.' : 'Proceed via Razorpay securely.'}
                        </div>
                      </div>
                      <button
                        onClick={() => setShowPaymentOptions(true)}
                        style={{ flexShrink: 0, padding: '7px 16px', border: '1.5px solid #ff6036', color: '#ff6036', borderRadius: '999px', fontSize: '12px', fontWeight: 700, background: 'transparent', cursor: 'pointer', whiteSpace: 'nowrap' }}
                      >
                        {paymentMethod === 'Card' ? '+ Add Card' : 'Change'}
                      </button>
                    </div>
                  ) : (
                    <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
                      {['Card', 'UPI', 'Net Banking', 'Cash on Delivery'].map(method => (
                        <div
                          key={method}
                          onClick={() => { setPaymentMethod(method); setShowPaymentOptions(false); }}
                          className="flex items-center gap-3 cursor-pointer rounded-xl"
                          style={{ padding: '11px 14px', border: paymentMethod === method ? '1.5px solid #ff6036' : '1px solid #e5e7eb', background: paymentMethod === method ? '#fff8f6' : '#fff' }}
                        >
                          <div style={{ width: 16, height: 16, borderRadius: '50%', border: `2px solid ${paymentMethod === method ? '#ff6036' : '#ccc'}`, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>
                            {paymentMethod === method && <div style={{ width: 8, height: 8, borderRadius: '50%', background: '#ff6036' }}></div>}
                          </div>
                          <span style={{ fontWeight: 600, fontSize: '13px', color: '#111' }}>{method}</span>
                        </div>
                      ))}
                    </div>
                  )}
                </div>

                {/* Continue to payment button */}
                <button
                  onClick={processPayment}
                  disabled={isProcessing}
                  style={{ width: '100%', padding: '15px', borderRadius: '12px', background: '#ff6036', color: '#fff', fontWeight: 700, fontSize: '15px', border: 'none', cursor: 'pointer', boxShadow: '0 4px 16px rgba(255,96,54,0.3)', letterSpacing: '0.01em' }}
                  onMouseEnter={e => e.currentTarget.style.background = '#eb4f28'}
                  onMouseLeave={e => e.currentTarget.style.background = '#ff6036'}
                >
                  {isProcessing ? 'Processing...' : 'Continue to payment'}
                </button>

              </div>
            </div>

            {/* â”€â”€ RIGHT PANEL: Cart Summary â”€â”€ */}
            <div className="flex-col hidden sm:flex" style={{ width: 300, flexShrink: 0, background: '#fff', borderLeft: '1px solid #e8e8e8', overflow: 'hidden' }}>
              <div className="flex-1 overflow-y-auto" style={{ padding: '20px' }}>

                {/* Cart header */}
                <div className="flex justify-between items-center mb-4">
                  <h3 style={{ margin: 0, fontSize: '16px', fontWeight: 700, color: '#111' }}>Cart ({cartItems.length})</h3>
                  <div style={{ width: 24, height: 24, borderRadius: '50%', border: '1.5px solid #ccc', display: 'flex', alignItems: 'center', justifyContent: 'center', color: '#888', fontSize: '12px', fontWeight: 700, cursor: 'pointer' }}>i</div>
                </div>

                {/* Cart items */}
                <div style={{ display: 'flex', flexDirection: 'column', gap: 16, marginBottom: 20 }}>
                  {cartItems.map((item, idx) => (
                    <div key={idx} className="flex items-center gap-3">
                      <div style={{ width: 60, height: 60, borderRadius: 12, background: '#f0f0f0', flexShrink: 0, overflow: 'hidden', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                        {item.image ? (
                          <img src={item.image} alt={item.name} style={{ width: '100%', height: '100%', objectFit: 'cover' }} />
                        ) : (
                          <span style={{ fontSize: 22 }}>ðŸ½ï¸</span>
                        )}
                      </div>
                      <div style={{ flex: 1, minWidth: 0 }}>
                        <div style={{ fontSize: '10px', color: '#2d9585', fontWeight: 700, marginBottom: 2 }}>Food</div>
                        <div style={{ fontWeight: 700, fontSize: '13px', color: '#111', whiteSpace: 'nowrap', overflow: 'hidden', textOverflow: 'ellipsis' }}>{item.name}</div>
                        <div style={{ fontSize: '12px', color: '#555', marginTop: 2 }}>â‚¹{((item.price || 0) * item.qty).toFixed(2)}</div>
                      </div>
                      <div style={{ width: 22, height: 22, borderRadius: '50%', background: '#f5f5f5', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '11px', fontWeight: 700, color: '#555', flexShrink: 0 }}>{item.qty}</div>
                    </div>
                  ))}
                </div>

                {/* Promo code */}
                <div style={{ marginBottom: 20 }}>
                  <div style={{ fontSize: '14px', fontWeight: 700, color: '#111', marginBottom: 10 }}>Promotion code</div>
                  <div className="flex items-center" style={{ background: '#f7f7f7', borderRadius: 10, padding: '4px 4px 4px 12px', border: '1px solid #e8e8e8' }}>
                    <input
                      type="text"
                      placeholder="Add promo code"
                      value={promoCode}
                      onChange={e => setPromoCode(e.target.value)}
                      style={{ flex: 1, border: 'none', background: 'transparent', outline: 'none', fontSize: '12px', color: '#555' }}
                    />
                    <button style={{ padding: '7px 14px', borderRadius: 8, background: promoCode ? '#ff6036' : '#e8e8e8', color: promoCode ? '#fff' : '#999', fontWeight: 700, fontSize: '12px', border: 'none', cursor: 'pointer', whiteSpace: 'nowrap', transition: 'all 0.2s' }}>Apply</button>
                  </div>
                </div>

                {/* Order total */}
                <div>
                  <div style={{ fontSize: '14px', fontWeight: 700, color: '#111', marginBottom: 12 }}>Order total</div>
                  <div className="flex justify-between" style={{ marginBottom: 8 }}>
                    <span style={{ fontSize: '12px', color: '#aaa' }}>Item Price</span>
                    <span style={{ fontSize: '12px', color: '#aaa' }}>â‚¹{itemTotal.toFixed(2)}</span>
                  </div>
                  <div className="flex justify-between" style={{ marginBottom: 8 }}>
                    <span style={{ fontSize: '12px', color: '#aaa' }}>Shipping</span>
                    <span style={{ fontSize: '12px', color: '#aaa' }}>{deliveryOption === 'Express' ? 'â‚¹1.99' : 'â‚¹0.00'}</span>
                  </div>
                  <div className="flex justify-between" style={{ marginBottom: 14 }}>
                    <span style={{ fontSize: '12px', color: '#aaa' }}>Platform Fee (5%)</span>
                    <span style={{ fontSize: '12px', color: '#aaa' }}>â‚¹{tax.toFixed(2)}</span>
                  </div>
                  <div style={{ height: 1, background: '#f0f0f0', marginBottom: 14 }}></div>
                  <div className="flex justify-between items-center">
                    <span style={{ fontSize: '14px', fontWeight: 700, color: '#111' }}>Total</span>
                    <span style={{ fontSize: '18px', fontWeight: 800, color: '#111' }}>â‚¹{totalToPay.toFixed(2)}</span>
                  </div>
                </div>

              </div>
            </div>

          </div>
        </div>
      )}


      {orderCompleteMsg && (
        <div className="fixed inset-0 z-[3000] bg-black/95 backdrop-blur-xl flex items-center justify-center p-8">
          <div style={{ backgroundColor: '#fff', width: '100%', maxWidth: '450px', borderRadius: '5rem', padding: '5rem', textAlign: 'center', boxShadow: '0 60px 150px rgba(0,0,0,0.8)' }}>
            <div style={{ width: '100px', height: '100px', backgroundColor: '#f0fdf4', color: '#16a34a', borderRadius: '50%', display: 'flex', alignItems: 'center', justifyCenter: 'center', margin: '0 auto 3rem auto', fontSize: '4rem', fontWeight: 900 }}>âœ“</div>
            <h2 style={{ fontFamily: 'var(--font-heading)', fontSize: '4rem', fontWeight: 900, color: '#3B2E28', marginBottom: '1rem', letterSpacing: '-0.02em' }}>Success!</h2>
            <p style={{ fontSize: '1.25rem', color: '#5C4A3E', opacity: 0.6, marginBottom: '4rem' }}>{orderCompleteMsg}</p>
            <button 
                onClick={() => { setOrderCompleteMsg(''); setIsCartOpen(false); }}
                style={{ backgroundColor: '#8C6A53', color: '#fff', border: 'none', width: '100%', padding: '2rem', borderRadius: '2.5rem', fontSize: '1.2rem', fontWeight: 800, letterSpacing: '0.2em', cursor: 'pointer' }}
                className="shadow-xl"
            >